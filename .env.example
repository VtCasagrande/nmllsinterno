# Configurações do Supabase (OBRIGATÓRIO)
# Estas variáveis são necessárias para a autenticação e acesso ao banco de dados
# Você encontra essas informações no painel do Supabase: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-chave-anon-key-aqui

# Configurações da aplicação
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Configurações de armazenamento (opcional)
# Se você estiver usando armazenamento do Supabase para uploads de arquivos
# Os nomes dos buckets que você criou no Supabase
NEXT_PUBLIC_STORAGE_DEVOLUCOES_BUCKET=devolucoes
NEXT_PUBLIC_STORAGE_PROFILE_BUCKET=profile-images

# Configurações de ambiente (não altere)
# Usado internamente para determinar o ambiente em que a aplicação está rodando
NODE_ENV=development

# INSTRUÇÕES DE CONFIGURAÇÃO:
# 1. Copie este arquivo para .env.local (desenvolvimento local)
# 2. Para produção, configure estas variáveis no seu servidor
# 3. Certifique-se de que as URLs do Supabase estejam corretas
# 4. As chaves do Supabase devem ser copiadas exatamente como aparecem no painel
#
# OBSERVAÇÕES:
# - Para o ambiente de produção, você deve configurar o domínio correto em NEXT_PUBLIC_APP_URL
# - Certifique-se de que o acesso ao Supabase está permitido a partir do domínio da sua aplicação
# - Se estiver tendo problemas de CORS, verifique as configurações no painel do Supabase

# Configuração para upload de arquivos (storage)
NEXT_PUBLIC_STORAGE_BUCKET=nmalls-storage

# Configurações para notificações (opcional)
# NEXT_PUBLIC_NOTIFICATION_SERVER=sua_url_do_servidor_de_notificacoes 