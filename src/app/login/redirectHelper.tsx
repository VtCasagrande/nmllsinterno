'use client';

import { useEffect } from 'react';
import { supabase } from '@/lib/supabase';
import { useRouter } from 'next/navigation';

export function RedirectHelper() {
  // Este componente foi completamente desativado
  // para evitar conflitos durante o processo de login
  
  // Não faz NADA, apenas existe para manter compatibilidade
  // com os imports existentes
  
  return null;
} 